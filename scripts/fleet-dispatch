#!/usr/bin/env bash
# fleet-dispatch shim — finds its .mjs next to the real script even when
# invoked through a symlink on PATH (~/.local/bin/fleet-dispatch).
set -euo pipefail
DIR="$(cd "$(dirname "$(readlink -f "$0")")" && pwd)"
exec node "$DIR/fleet-dispatch.mjs" "$@"
